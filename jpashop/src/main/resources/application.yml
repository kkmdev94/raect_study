spring:
  datasource:
#    url: jdbc:h2:tcp://localhost/~/jpashop;
    url: jdbc:h2:tcp://localhost/~/testcase; #HOME
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100 # 25.12.17 fetch Join paging 작업을 위한 한계돌파. / 글로벌 적용.

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace