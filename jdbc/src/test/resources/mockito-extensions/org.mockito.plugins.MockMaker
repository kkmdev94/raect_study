# [이 파일의 역할과 이유]
# 최신 JDK(17+) 환경에서 Mockito는 동적으로 Java 에이전트를 로드하여
# final 클래스/메서드를 모의(mock) 객체로 만드는데, 이 방식에 대해 경고를 보냅니다.
#
# 이 파일은 Mockito 확장 설정 파일입니다.
# Mockito가 테스트 시작 시점부터 'inline' 모의(mock) 생성기를 사용하도록 명시적으로 지정합니다.
# 이렇게 하면 런타임에 동적으로 에이전트를 로드할 필요가 없어지므로, 관련 경고가 사라집니다.
mock-maker-inline