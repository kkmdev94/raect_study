package cofficeShop;

public class Manager {
    // 커피를 만들어서 제공해 준다.
    public String  coffie(Coffie coffie) {
        String key = coffie(coffie);
        return key;
    }

    public void coffie() {
    }
    // 어떤 커피던 받아올 수 있다 새로운 커피가 추가 되도 매니저는 바뀌지 않는다
    // 매장에서 매니저를 이용해 커피를 제공한다.
}
